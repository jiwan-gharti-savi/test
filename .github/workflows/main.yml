name: Custom Hook Example

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Execute custom hook script
      run: |
        
        .github/hooks/run-tests-and-coverage.sh